services:
  dev-environment:
    build:
      dockerfile: ./docker/Dockerfile
      target: development
    volumes:
      - .:/pytorch-project
    env_file: ".env"
  train-model:
    build:
      dockerfile: ./docker/Dockerfile
      target: train-model
    volumes:
      - .:/pytorch-project
    env_file: ".env"
